// Mengimpor fungsi sum dari modul math.js
const sum = require('./math');

// Menggunakan fungsi sum untuk menjumlahkan dua angka
const result = sum(5, 7);

// Mencetak hasil ke konsol
console.log(`The sum of 5 and 7 is: ${result}`);
